<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ (API 28+) blocks cleartext (HTTP) traffic app-wide by default.
  ApiEnvironment.local's default (http://10.0.2.2:9850/ - the Android
  emulator's alias for the host machine, see lib/core/config/api_environment.dart)
  is plain HTTP for local dev against a backend run without TLS. This scopes
  the cleartext exception to just that dev host, plus localhost/127.0.0.1 for
  a host-machine backend or ADB reverse-forwarding - everything else
  (including ApiEnvironment.production's real HTTPS API, and any
  ApiEnvironment.custom URL a developer enters in Profile > Developer
  settings) stays subject to the OS's default HTTPS-only policy. A custom
  http:// URL to any other host (a LAN IP, say) is silently blocked here -
  use https:// or a tunnel for that instead of widening this allowlist.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
